class DrawingTypeTwo implements DrawingType {
    public void drawCircle(int x, int y, int radius) {
        System.out.println("Menggambar lingkaran TypeTwo di (" + x + ", " + y + ") dengan radius " + radius);
    }
}