class DrawingTypeOne implements DrawingType {
    public void drawCircle(int x, int y, int radius) {
        System.out.println("Menggambar lingkaran TypeOne di (" + x + ", " + y + ") dengan radius " + radius);
    }
}